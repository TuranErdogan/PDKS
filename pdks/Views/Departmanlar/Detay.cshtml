@model pdks.Models.Departmanlar

@{
    ViewBag.Title = "Detay";
}

<h2>Detay</h2>

<div>
    <h4>Departmanlar</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DepartmanAdi)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DepartmanAdi)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Güncelle", "Guncelle", new { id = Model.Id }) |
    @Html.ActionLink("Sil", "Sil", new { id = Model.Id }) |
    @Html.ActionLink("Kayıtlara Dön", "Index")
</p>
